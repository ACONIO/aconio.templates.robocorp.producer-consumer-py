name: Pull Request

on:
  pull_request:
    branches:
      - main
      - "release/**"

permissions:
  contents: read

jobs:
  CI:
    uses: aconio/aconio.common.workflows/.github/workflows/ci.yml@main
    secrets: inherit
